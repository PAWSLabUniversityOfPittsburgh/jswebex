var mysql = require('mysql');

var conn = mysql.createConnection({
    host: "localhost",
    user: "root",
    password: "15213",
    database: "aggregate"
    }
);

var queryString = "SELECT display_name FROM aggregate.ent_topic WHERE domain = 'py'";
conn.query(queryString, function (error,results)
{
    if(error)
    {
        throw error;
    }
    else
    {
        a = results;
        console.log(a);
    }
}
)
conn.end();